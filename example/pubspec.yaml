name: autoupdate_example
description: Example app demonstrating the autoupdate package
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.0-154.0.dev
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  autoupdate:
    path: ../
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
